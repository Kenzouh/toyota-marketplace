@model ToyotaMarketplace.Models.ViewModels.Admin.Vehicle.AddVehicleViewModel

@using System.Text.Json

@{
	Layout = "_Layout";
}

<div class="min-h-screen bg-zinc-100 py-10">

    <div class="mx-auto max-w-7xl px-6">

        <!-- PAGE HEADER -->
        <div class="mb-8">

            <div class="flex items-center gap-4">

                <div class="flex h-14 w-14 items-center justify-center rounded-2xl bg-black shadow-lg">

                    <img src="~/icons/car.svg"
                         alt="vehicle"
                         class="h-7 w-7 invert" />

                </div>

                <div>

                    <h1 class="text-4xl font-black tracking-tight text-zinc-900">
                        Add Vehicle
                    </h1>

                    <p class="mt-1 text-zinc-500">
                        Create and manage complete vehicle specifications and features.
                    </p>

                </div>

            </div>

        </div>

        <form method="post"
              enctype="multipart/form-data"
              class="space-y-8">

            <!-- ================================================== Vehicle Hero Section ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Vehicle Hero Section
                    </h2>

                    <p class="mt-2 text-sm text-zinc-300">
                        Main vehicle identity, branding, pricing, and appearance.
                    </p>

                </div>

                <div class="grid gap-6 p-8 lg:grid-cols-2">

                    <!-- Vehicle Image -->
                    <div class="space-y-2">
                        
                        <!-- VehicleImg -->
                        <label class="text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Vehicle Images
                        </label>

                        <div class="rounded-2xl border-2 border-dashed border-zinc-300 bg-zinc-50 p-8 text-center transition hover:border-red-500 hover:bg-red-50">

                            <input asp-for="VehicleImages",
                                   type="file"
                                   multiple
                                   class="w-full text-sm text-zinc-600" />

                            <p class="mt-3 text-sm text-zinc-400">
                                Upload multiple vehicle images
                            </p>

                        </div>

                    </div>

                    <!-- Right Side -->
                    <div class="grid gap-5">

                        <div>

                            <!-- VehicleName -->
                            <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                                Vehicle Name
                            </label>

                            <input asp-for="VehicleName",
                                   type="text"
                                   placeholder="Toyota Vios"
                                   class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                        </div>

                        <!-- Vehicle Type -->
                        <div class="grid gap-5 md:grid-cols-3">

                            <div>

                                <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                                    Vehicle Type
                                </label>

                                <!-- VehicleType: retrieved from DB, and displayed -->
                                <select id="vehicleTypeDropdown"
                                        asp-for="VehicleTypeId"
                                        asp-items="Model.VehicleTypes"
                                        class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100">

                                    <option value="">
                                        Select Vehicle Type
                                    </option>

                                </select>

                            </div>

                            <div>

                                <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                                    Vehicle Model
                                </label>

                                <!-- VehicleModel: retrieved from DB and displayed -->
                                <select id="vehicleModelDropdown"
                                        asp-for="VehicleModelId"
                                        asp-items="Model.VehicleModels"
                                        class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100">

                                    <option value="">
                                        Select Vehicle Model
                                    </option>

                                </select>

                            </div>

                            <div>

                                <!-- PowerTrainType -->
                                <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                                    PowerTrain
                                </label>

                                <select asp-for="PowerTrainId"
                                        asp-items="Model.PowerTrains"
                                        class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100">

                                    <option value="">
                                        Select PowerTrain
                                    </option>

                                </select>

                                <div>

                                    <!-- FuelName -->
                                    <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                                        Fuel Type
                                    </label>

                                    <select asp-for="FuelTypeId"
                                            asp-items="Model.FuelTypes"
                                            class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100">

                                        <option value="">Select Fuel Type</option>

                                    </select>

                                </div>

                            </div>

                        </div>

                        <!-- Wheels + SRP -->
                        <div class="grid gap-5 md:grid-cols-2">

                            <div>

                                <!-- Wheel -->
                                <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                                    Wheels
                                </label>

                                <input asp-for="Wheel"
                                       type="number"
                                       placeholder="4"
                                       class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                            </div>

                            <div>


                                <!-- VehicleSRP -->
                                <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                                    SRP
                                </label>

                                <input asp-for="VehicleSRP"
                                       type="number"
                                       step="0.01"
                                       placeholder="1250000"
                                       class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                            </div>

                        </div>

                    </div>

                    <!-- Colors -->
                    <div class="lg:col-span-2">

                        <label class="mb-3 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Available Colors
                        </label>

                        <div class="overflow-hidden rounded-2xl border border-zinc-200 bg-zinc-50">

                            <!-- TOP SECTION -->
                            <div class="flex flex-wrap items-center justify-between gap-4 border-b border-zinc-200 bg-white px-6 py-4">

                                <!-- LEFT -->
                                <div class="flex items-center gap-4">

                                    <!-- VehicleColorCategory -->
                                    <div>

                                        <label class="mb-2 block text-xs font-bold uppercase tracking-wide text-zinc-400">
                                            Color Category
                                        </label>

                                        <select id="colorCategoryDropdown"
                                                class="rounded-xl border border-zinc-300 bg-zinc-50 px-4 py-2 text-sm font-medium outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100">

                                            <option value="">
                                                Select Category
                                            </option>

                                            @foreach (var category in Model.VehicleColorCategories)
                                            {
                                                <option value="@category.Value">
                                                    @category.Text
                                                </option>
                                            }

                                        </select>

                                    </div>

                                </div>

                                <!-- RIGHT -->
                                <div class="text-right">

                                    <!-- VehicleColorName -->
                                    <p id="selectedColorName"
                                       class="text-xs font-bold uppercase tracking-wide text-zinc-400">

                                        No Color Selected

                                    </p>

                                    <!-- Divider -->
                                    <div class="my-2 h-px w-full bg-zinc-200"></div>

                                    <!-- VehicleColorHexadecimal -->
                                    <p id="selectedColorHex"
                                       class="font-mono text-lg font-black text-zinc-900">

                                        #000000

                                    </p>

                                    <input type="hidden"
                                           id="selectedVehicleColorId"
                                           name="VehicleColorIds" />
                                </div>

                            </div>

                            <!-- COLOR AREA -->
                            <div class="flex items-center overflow-hidden">

                                <!-- Left Arrow -->
                                <button type="button"
                                        class="flex h-20 w-16 items-center justify-center border-r border-zinc-200 text-2xl font-black text-zinc-500 transition hover:bg-zinc-100 hover:text-black">

                                    ‹

                                </button>

                                <!-- Dynamic Color Circles -->
                                <div id="colorContainer"
                                     class="flex flex-1 items-center gap-4 overflow-x-auto px-6 py-5">
                                </div>

                                <!-- Plus Button -->
                                <div class="border-l border-zinc-200">

                                    <button type="button"
                                            class="flex h-20 w-16 items-center justify-center text-3xl font-light text-zinc-500 transition hover:bg-red-50 hover:text-red-600">

                                        +

                                    </button>

                                </div>

                                <!-- Right Arrow -->
                                <button type="button"
                                        class="flex h-20 w-16 items-center justify-center border-l border-zinc-200 text-2xl font-black text-zinc-500 transition hover:bg-zinc-100 hover:text-black">

                                    ›

                                </button>

                            </div>

                        </div>

                    </div>

                    <!-- Disclaimer -->
                    <div class="lg:col-span-2">

                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Disclaimer
                        </label>

                        <textarea asp-for="Disclaimer"
                                  rows="4"
                                  placeholder="Actual unit specifications may vary depending on region and model variant."
                                  class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none transition focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    </div>

                </div>

            </section>



            <!-- ================================================== Performance ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Performance
                    </h2>

                </div>

                <div class="grid gap-5 p-8 md:grid-cols-2">


                    <!-- Engine -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Engine
                        </label>

                        <input asp-for="Engine"
                               type="text"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- TransmissionName -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Transmission
                        </label>

                        <select asp-for="TransmissionTypeId"
                                asp-items="Model.TransmissionTypes"
                                class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100">

                            <option value="">
                                Select Transmission
                            </option>

                        </select>
                    </div>

                    <!-- Drivetrain -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Drivetrain
                        </label>

                        <input asp-for="Drivetrain"
                               type="text"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- Chassis -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Chassis
                        </label>

                        <input asp-for="Chassis"
                               type="text"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- ModeName -->
                    <div class="md:col-span-2">

                        <label class="mb-3 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Drive Modes
                        </label>

                        <div class="flex flex-wrap gap-4 rounded-2xl border border-zinc-200 bg-zinc-50 p-5">

                        @if (Model.DriveModes != null && Model.DriveModes.Any())
                        {
                            @foreach (var driveMode in Model.DriveModes)
                            {
                                <label class="flex items-center gap-2">
                                    <input type="checkbox"
                                           name="DriveModeIds"
                                           value="@driveMode.Value" />

                                    <span>@driveMode.Text</span>
                                </label>
                            }
                        }
                        else
                        {
                            <div class="flex w-full justify-center py-4">
                                <span class="text-sm italic text-zinc-400">
                                    No data yet
                                </span>
                            </div>
                        }
                        </div>

                    </div>

                </div>

            </section>



            <!-- ================================================== Technical Specifications ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Technical Specifications
                    </h2>

                </div>

                <div class="grid gap-5 p-8 md:grid-cols-2">

                    <!-- Suspension -->
                    <input asp-for="Suspension" type="text" placeholder="Suspension" class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                    <!-- SteeringSystemName -->
                    <input asp-for="SteeringSystemName" type="text" placeholder="Steering System Name" class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                    <!-- SteeringTypeName -->
                    <input asp-for="SteeringTypeName" type="text" placeholder="Steering Type Name" class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                    <!-- PowerSteeringTypeName -->
                    <select 
                        asp-for="PowerSteeringTypeId"
							asp-items="Model.PowerSteeringTypes"
                        class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100">

                        <option value="">Select Power Steering Type</option>

                    </select>

                    <!-- Front Brake -->
                    <select asp-for="FrontBrakeTypeId"
                            asp-items="Model.FrontBrakeTypes"
                            class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100">

                        <option value="">
                            Select Front Brake Type
                        </option>

                    </select>

                    <!-- Rear Brake -->
                    <select asp-for="RearBrakeTypeId"
                            asp-items="Model.RearBrakeTypes"
                            class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100">

                        <option value="">
                            Select Rear Brake Type
                        </option>

                    </select>

                    <!-- MinimumTurningRadius -->
                    <input asp-for="MinimumTurningRadius"
                           type="number"
                           step="0.01"
                           placeholder="Minimum Turning Radius"
                           class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                    <!-- BatteryName -->
                    <select asp-for="BatteryTypeId"
                            asp-items="Model.BatteryTypes"
                            class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100">

                        <option value="">Select Battery Type</option>

                    </select>

                </div>

            </section>



            <!-- ================================================== Dimensions & Capacity ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Dimensions & Capacity
                    </h2>

                </div>

                <div class="grid gap-5 p-8 md:grid-cols-3">

                    <!-- DimensionX -->
                    <div>

                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Width
                        </label>

                        <input asp-for="DimensionX"
                               type="number"
                               step="0.01"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />

                    </div>

                    <!-- DimensionY-->
                    <div>

                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Height
                        </label>

                        <input asp-for="DimensionY"
                               type="number"
                               step="0.01"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- DimensionZ -->
                    <div>

                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Length
                        </label>

                        <input asp-for="DimensionZ"
                               type="number"
                               step="0.01"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- GroundClearance -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Ground Clearance
                        </label>

                        <input asp-for="GroundClearance"
                               type="number"
                               step="0.01"
                               placeholder="198.50"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- SeatingCapacity -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Seating Capacity
                        </label>

                        <input asp-for="SeatingCapacity"
                               type="number"
                               placeholder="7"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- PayloadCapacity -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Payload Capacity
                        </label>

                        <input asp-for="PayloadCapacity"
                               type="number"
                               step="0.01"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                    <!-- FuelCapacity -->
                    <div>
                        <label class="mb-2 block text-sm font-bold uppercase tracking-wide text-zinc-500">
                            Fuel Capacity (Liters)
                        </label>

                        <input asp-for="FuelCapacity"
                               type="number"
                               step="0.01"
                               placeholder="55.00"
                               class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100" />
                    </div>

                </div>

            </section>



            <!-- ================================================== Exterior Features ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Exterior Features
                    </h2>

                </div>

                <div class="grid gap-5 p-8">

                    <!-- Exterior -->
                    <textarea asp-for="Exterior"
                              placeholder="Exterior Features"
                              rows="4"
                              class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- TireDiskWheel -->
                    <textarea asp-for="TireDiskWheel"
                              placeholder="Tire Disk Wheel"
                              rows="4"
                              class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                </div>

            </section>



            <!-- ================================================== Interior Features ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Interior Features
                    </h2>

                </div>

                <div class="grid gap-5 p-8 md:grid-cols-2">

                    <!-- Interior -->
                    <textarea asp-for="Interior"
                              rows="3" placeholder="Interior" 
                              class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- MeterCluster -->
                    <textarea asp-for="MeterCluster"
                              rows="3" placeholder="Meter Cluster" 
                              class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- MultiInfoDisplay -->
                    <textarea asp-for="MultiInfoDisplay" 
                              rows="3" placeholder="Multi Info Display"
                              class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- Audio -->
                    <textarea asp-for="Audio" 
                              rows="3" placeholder="Audio" 
                              class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- Ignition -->
                    <textarea asp-for="Ignition" 
                              rows="3" placeholder="Ignition" 
                              class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- Function -->
                    <textarea asp-for="Function" 
                              rows="3" placeholder="Function" 
                              class="rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                </div>

            </section>



            <!-- ================================================== Safety Features ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Safety Features
                    </h2>

                </div>

                <div class="grid gap-5 p-8">

                    <!-- Safety -->
                    <textarea asp-for="Safety"
                              placeholder="Safety"
                              rows="4"
                              class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- AntiLockBrakeSystem -->
                    <textarea asp-for="AntiLockBrakeSystem"
                              placeholder="Anti Lock Brake System"
                              rows="4"
                              class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>
                </div>

            </section>



            <!-- ================================================== Additional Information ================================================== -->

            <section class="overflow-hidden rounded-[2rem] bg-white shadow-xl">

                <div class="border-b border-zinc-200 bg-black px-8 py-6">

                    <h2 class="text-3xl font-black text-white">
                        Additional Information
                    </h2>

                </div>

                <div class="grid gap-5 p-8">

                    <!-- ModelNote -->
                    <textarea asp-for="ModelNote"
                              placeholder="Model Note"
                              rows="4"
                              class="w-full rounded-2xl border border-zinc-300 bg-zinc-50 px-5 py-4 outline-none focus:border-red-500 focus:ring-4 focus:ring-red-100"></textarea>

                    <!-- Auto Date Posted -->
                    <div class="rounded-2xl border border-zinc-200 bg-zinc-50 p-5">

                        <p class="text-sm uppercase tracking-wide text-zinc-400">
                            Date Posted
                        </p>

                        <h3 class="mt-2 text-lg font-bold text-zinc-900">
                            Automatically Generated
                        </h3>

                    </div>

                </div>

            </section>



            <!-- Submit -->
            <div class="sticky bottom-6 z-50 flex justify-end">

                <button type="submit"
                        class="rounded-2xl bg-red-600 px-8 py-4 text-lg font-bold text-white shadow-2xl transition hover:scale-105 hover:bg-red-700">

                    Save Vehicle

                </button>

            </div>

        </form>

    </div>

</div>

@section Scripts {

    <script>

        // All colors from controller
        const vehicleColors =
        @Html.Raw(JsonSerializer.Serialize((object)ViewBag.VehicleColorsData));

        // Elements
        const colorCategoryDropdown =
            document.getElementById("colorCategoryDropdown");

        const colorContainer =
            document.getElementById("colorContainer");

        const selectedColorName =
            document.getElementById("selectedColorName");

        const selectedColorHex =
            document.getElementById("selectedColorHex");



        // =======================  Render Colors =======================

        function renderColors(categoryId)
        {
            // Clear existing circles
            colorContainer.innerHTML = "";

            // Filter colors by category
            const filteredColors = vehicleColors.filter(color =>
                color.VehicleColorCategoryId == categoryId
            );

            // Generate circles
            filteredColors.forEach(color =>
            {
                const button = document.createElement("button");

                button.type = "button";

                button.className =
                    "group relative h-14 w-14 rounded-full border-4 border-white shadow-lg transition hover:scale-125";

                // Circle color
                button.style.backgroundColor =
                    color.VehicleColorHexadecimal;

                // Checkmark
                button.innerHTML = `
                    <span class="absolute inset-0 flex items-center justify-center text-white opacity-0 transition group-hover:opacity-100">
                        ✓
                    </span>
                `;

                // When clicked
                button.addEventListener("click", () =>
                {
                    selectedColorName.innerText =
                        color.VehicleColorName;

                    selectedColorHex.innerText =
                        color.VehicleColorHexadecimal;

                    document.getElementById("selectedVehicleColorId").value = color.VehicleColorId;
                });

                // Append
                colorContainer.appendChild(button);
            });
        }



        // ======================= Dropdown Change =======================

        colorCategoryDropdown.addEventListener("change", function ()
        {
            renderColors(this.value);
        });


            // =======================  Vehicle Models Dynamic Dropdown =======================

            // All vehicle models from controller
            const vehicleModels =
            @Html.Raw(JsonSerializer.Serialize((object)ViewBag.VehicleModelsData));

            // Dropdowns
            const vehicleTypeDropdown =
                document.getElementById("vehicleTypeDropdown");

            const vehicleModelDropdown =
                document.getElementById("vehicleModelDropdown");



            // ======================= Render Vehicle Models ======================

            function renderVehicleModels(vehicleTypeId)
            {
                // Clear old options
                vehicleModelDropdown.innerHTML = "";

                // Default option
                const defaultOption = document.createElement("option");

                defaultOption.value = "";

                defaultOption.text =
                    "Select Vehicle Model";

                vehicleModelDropdown.appendChild(defaultOption);



                // Filter models based on vehicle type
                const filteredModels = vehicleModels.filter(model =>
                    model.VehicleTypeId == vehicleTypeId
                );

                // Create options
                filteredModels.forEach(model =>
                {
                    const option = document.createElement("option");

                    option.value =
                        model.VehicleModelId;

                    option.text =
                        model.ModelName;

                    vehicleModelDropdown.appendChild(option);
                });
            }

            vehicleTypeDropdown.addEventListener("change", function ()
            {
                renderVehicleModels(this.value);
            });


    </script>
}